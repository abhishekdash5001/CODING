ECS manages the cotainer like run 1 more if crases rety 
EC2 where our server is rruning we manage it like ram os auuscallinng we decide config whic node to install


fargate aws run theserver for use withoout showing them it is hanled by aws
